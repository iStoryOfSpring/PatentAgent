# PatentAgent 导师演示流程

## 演示目标

用一条完整链路展示系统不是静态界面：专利文件经过格式识别和字段审计形成不可变数据版本，会话绑定该版本，总 Agent 根据自然语言选择工具，最终报告保留数据、参数、方法和限制。

## 演示前准备

1. 执行 `bash start.sh`，确认顶部后端状态为在线。
2. 在“设置”中连接一个支持工具调用和结构化输出的 LLM 配置。
3. 准备 WoS/Derwent TXT、Google Patents JSONL 或 USPTO XML/JSON 官方导出样例。

API Key 和敏感 Header 只保存在当前后端进程内存，重启后需要重新输入。

## 推荐的五分钟演示

1. 打开“数据集”，选择多个原始文件并保持“自动识别”，点击“上传并导入”。
2. 展示导入进度、识别格式、记录数和数据质量警告。
3. 将完成的数据版本绑定到当前会话；说明更换版本时旧证据会标记为过期。
4. 返回“智能分析”，从九类能力卡选择“分析近五年的专利公开趋势”。
5. 展示 Agent 的分析计划、实际工具步骤、图表、方法、字段覆盖率和最终总结。
6. 追问“这个结论受哪些数据限制影响”，展示同版本证据复用。
7. 打开“报告”，生成当前会话 HTML 报告并在新窗口打开。

## 建议强调的边界

- 系统自动化的是数据清洗、描述统计、工程筛查、图表和证据整理。
- 价值模块是当前数据集内的相对工程评分，不是财务估值。
- 缺少权利要求或实时法律状态时，只能做相关专利初筛，不构成 FTO 或侵权意见。
- 能力卡不是六个独立 Agent；系统始终由一个总 Agent 根据问题和数据门槛选择最小必要工具集。

## 无外部 LLM 时的备用演示

可以进入“能力与工具”页面直接执行可用工具，展示数据版本、图表和方法证据。完整自然语言编排仍需要连接通过工具闭环探测的 LLM。
